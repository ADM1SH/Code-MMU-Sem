/*
    Question 3: Variable Scope

    a) A local variable is declared inside a function or block and is only accessible within that scope. 
       Its lifetime ends when the function or block finishes. A global variable is declared outside all functions 
       and is accessible throughout the entire program. Its lifetime lasts for the entire duration of the program.

    b) A static local variable is declared inside a function using the 'static' keyword. 
       It retains its value between function calls and exists for the lifetime of the program, 
       but its scope is still limited to the function in which it is defined.
*/